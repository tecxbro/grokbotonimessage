# Header assets: transfer status

The original package contains `hands.png` and `study.png`. Their binary upload did not complete in this GitHub transfer. They remain in `grokbot-live-task-cards-v1.zip` supplied in the ChatGPT conversation. No replacement artwork has been generated.

Restore those exact two files here before using `header: "hands"` or `header: "study"`. Their expected SHA-256 values are in `../../MANIFEST.sha256.json`. Until then, use the existing supported `header: "none"` setting. The research and checks example payloads still name the original assets and have intentionally not been rewritten.

All 56 original text/code/configuration/test/evidence files were transferred separately. The recorded evidence describes the original complete package, not a fresh browser test of this asset-incomplete GitHub copy.
