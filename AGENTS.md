# Improving friend's bot: fresh setup and neutral features

## Isolation and ownership

Changes belong on `improving-friends-bot`, not main or the release-fix branch.
Inspect the actual checkout and preserve unrelated work and all live installations.
The exact branch maintenance scope is recorded in
`docs/worktrees/friend-scratch/FILES.json`; historical lane locations and immutable
reviewed commits remain historical evidence, not this branch's checkout paths.
No wildcard ownership, silent shared-contract change, or frozen-foundation reset.

## Source authority

Use pinned Node/npm/SDK contracts and actual repository APIs. Official Photon
source/documentation and the pinned public declarations determine valid calls;
skills are usage guidance, not permission to change infrastructure. Photon website
examples may differ from CLI 2.2.0; record the verified version-specific contract.
Do not invent a native Grok registration API or private worker-activity feed.

## Documentation cadence

Maintain the fresh setup README, ROUTINE, source record and review evidence with
meaningful implementation changes. The root README is the first entry point.
Keep planned, implemented, tested and target-deployed behavior separate. The
neutral feature skill remains about selection, inputs, targets and results only.
The older gateway profile stays backward-compatible but is not fresh setup.

## Exported APIs and comments

Document exported helpers and non-obvious authorization, transaction, ordering,
cancellation, activity and unknown-outcome boundaries. Preserve the original
feature implementations, strict schemas, resource validation and child journal.
Use the existing executor and one shared Spectrum owner; no parallel outbox or
private feature-table bypass. A queued operation is not evidence of delivery.

## Evidence and completion

Run focused setup tests, feature-only checks, build/schema/skill drift checks, full
regressions, installed-archive tests and exact documentation/ownership verification.
Do not weaken assertions or mark missing implementations skipped. Update only the
assembled candidate digest; never move the historical foundation checkpoint.
Record exact source/artifact identities, command results and unverified boundaries.
A full offline build is not an installed native routine or an iPhone observation.

## Operating boundary

For an owner's explicit “connect me to iMessage” request, use
`packages/photon-features/scratch-setup/README.md` and the supplied program on the
bot's Linux cloud VM. This is authorized connection setup, not an assignment to
write another adapter. The development-time tests do not authorize a real account
change, installation or unsolicited test send; they also do not negate a separate
explicit owner request to install/connect.

Use one persistent hosted Spectrum runtime, the bot's real native webhook routine,
and the actual authenticated helper. No gateway discovery, transcript polling,
second SDK client, replacement model, extra bot or role-topology instruction.
Keep login, private native binding and supervision in setup; do not add personality,
speaking-style or delegation policy to the feature skill or routine instructions.

Ordinary replies to real authorized incoming work belong in the originating conversation. They are external effects, not offline tests. Preserve current
permissions and do not disable Auto Review, disguise effects, buy lines, rotate
working secrets or change billing merely to make connection tests pass.
Missing host persistence, native routine access or device evidence stays unverified.
