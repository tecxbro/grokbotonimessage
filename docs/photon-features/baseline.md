# WT-00 baseline

Inspected 2026-09-08. Assigned repository: https://github.com/tecxbro/grokbotonimessage (origin URL ends `.git`). Absolute checkout: `/Users/darshan/Documents/ChatGPT/grokbotxphoton`. Current branch at start: `main`; HEAD: `24468391b57f028b4b881ddbf71efab3a49a6f73`.

Comparison branch: `origin/main`, exact commit `24468391b57f028b4b881ddbf71efab3a49a6f73`, freshly fetched successfully during this task. Current merge-base at baseline: `24468391b57f028b4b881ddbf71efab3a49a6f73`. Upstream: `origin/main`; ahead/behind: 0/0. These are baseline observations, not a promise that the remote will remain unchanged.

Original worktree creation base: **unknown**. This is the only entry in `git worktree list --porcelain`. Retained HEAD reflog includes earlier branch creation, switching, merging, and a later repository-history replacement. It does not establish a separate WT-00 worktree creation event or prove that the present root commit was its creation base. No branch/worktree was created, switched, reset, rebased or force-pushed by WT-00.

Initial dirty files: `package.json` and `package-lock.json`. The manifest added `spectrum-ts: ^12.8.0`; the lockfile contained its installation and unrelated lock normalization. No other tracked or untracked changes were observed. Those initial files were preserved under `/tmp/wt00-baseline` before work. WT-00's commit uses a lock generated from the committed root manifest plus WT-00's workspace changes; the pre-existing root Spectrum dependency remains only in the working copy. See rollout for the final index/working-copy verification.

Existing software is the Grok Bot CLI 0.2.2, ESM JavaScript, npm lockfile v3. `src/gateway.js` supports existing gateway prompt submission and `src/commands.js` selects file/gateway backends. The file backend's records are agent configuration, not a compatible durable Photon inbox/outbox. They remain unchanged. Existing gateway submission is a possible wake adapter seam, with task binding and credential boundary still unverified. No transcript polling is introduced.

No package workspace, TypeScript build, Photon Feature Runtime, SQLite messaging store, fuller voice-policy file, or repo AGENTS.md was found in the assigned checkout. `/workspace/grok-photon-proof` is absent in this environment. The macOS checkout and that historical deployment path are not treated as the same host. No other repository, hosted deployment or iMessage-agent-render was modified.

Initial tools: Node v23.11.0 and npm 10.9.2. Existing tests: `npm test`, 31 passed, 0 failed. Selected F0 toolchain: Node 24.13.0, npm 10.9.2, TypeScript 5.9.3, spectrum-ts and bundled @spectrum-ts/core/@spectrum-ts/imessage 12.8.0, Zod 4.5.4, @types/node 24.10.1. Node 24 supplies SQLite; Node 23 remains the shell's global default and was not replaced. Use Node 24 on PATH for package commands.

Compatibility findings: Spectrum 12.8.0 shipped declaration errors under full library checking. `skipLibCheck: true` suppresses dependency-body errors only. `exactOptionalPropertyTypes: true` also broke the provider's conditional definition inference at public call sites; false restores typed app/space/message narrowing. `strict: true` and `noUncheckedIndexedAccess: true` remain enabled. No SDK files, private imports, ambient shims or provider casts were used to force the probes. Node's SQLite API reports an experimental warning in 24.13.0. `npm install --ignore-scripts` reported 18 moderate advisories; no automatic audit fixes were applied. No fresh-install audio binary availability is claimed.
