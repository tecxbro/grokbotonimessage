# Integration handoff

Status: in progress.

The registered integration branch starts from F0 and has completed identity,
source, lane-document, and immutable-commit review. No lane implementation is
integrated at this documentation checkpoint. No package is installed or activated,
and no provider/account/device behavior has been exercised.

See `included-commits.json` for selected inputs and `CHANGE-REQUESTS.md` for the
known shared seams that must remain blocked until implemented and retested.
